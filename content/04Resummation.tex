\chapter{Resummation}
In this chapter we discuss the resummation of logarithmically enhanced contributions in perturbative QCD, focusing on the semi-numerical frameworks \textsc{CAESAR} (\emph{Computer Automated Expert Semi-Analytical Resummer})~\cite{Banfi:2004yd} and \textsc{ARES} (\emph{Automated Resummation for Event Shapes})~\cite{Banfi:2014sua}. These frameworks provide systematic methods to resum large logarithms that arise when fixed-order predictions become unreliable for a class of IRC safe observables known as event shapes. To illustrate the origin of such logarithms, we first examine a fixed-order calculation for the event shape known as thrust. We discuss what event shapes are, describing examples which we will use in Chapter~5. We then outline the \textsc{CAESAR} framework - firstly illustrated as an idea and then implemented into a code -  which automates resummation to next-to-leading logarithmic (NLL) accuracy, and the \textsc{ARES} framework, which extends this to next-to-next-to-leading logarithmic (NNLL) precision. \textsc{CAESAR} has been applied to both $e^+e^-$ and hadronic collisions~\cite{Banfi:2004yd, Banfi:2004nk}, while \textsc{ARES} has been developed primarily for $e^+e^-$ collisions~\cite{Banfi:2014sua, Banfi:2018mcq, Arpino:2019ozn}. In Chapter~5, we present the first application of \textsc{ARES} to hadronic collisions, including the treatment of initial-state radiation. \question{jet-veto for ARES also?}


\section{Motivation: Thrust and the Need for Resummation}
The event shape \emph{thrust}~\cite{Farhi:1977sg, Brandt:1964sa} is widely used in the literature and at the Large Electron-Positron Collider (LEP) for precise determinations of the strong coupling $\alpha_s$~\cite{Aglietti:2025jdj, Abbate:2010xh} \question{Unsure if these are the correct references to use due to the controversy?}. Thrust is defined as
\begin{equation}
\label{eq:thrust}
	T \equiv \max_{\vec{n}} \frac{\sum_j |\vec{p}_j \cdot \vec{n}|}{\sum_j |\vec{p}_j|}\,, \quad \tau \equiv 1 - T \,,
\end{equation}
where $\vec{p}_j$ denotes the three-momentum of particle $j$ in the final state, and the unit vector $\vec{n}$ that maximises this quantity is referred to as the \emph{thrust axis}. For $e^+e^- \to \mathrm{hadrons}$, thrust provides a measure of how ``pencil-like'' an event is: $T \simeq 1$ corresponds to two narrow, back-to-back jets, while smaller values of $T$ indicate more spherically distributed final states. It is conventional to work with the variable $\tau$, as defined in Eq.~\eqref{eq:thrust}, such that the two-jet limit corresponds to the Born-level configuration with a perfectly back-to-back $q\bar{q}$ pair ($\tau \to 0$), while a spherically distributed multi-particle event occurs near $\tau \to 1/2$, as illustrated in Fig.~\eqref{fig:thrust}.
\begin{figure}[h]
\begin{tikzpicture}[scale=0.9, >=Stealth]
\begin{scope}
\node at (0,2.5) {$\tau = 0$};

\draw[thick] (-3.5,0) -- (3.5,0);
\node[left]  at (-3.5,0) {$e^+$};
\node[right] at (3.5,0) {$e^-$};

\fill (0,0) circle (2pt);

\draw[very thick, ->] (0,0) -- (1.5,1.5) node[above right] {$q$};
\draw[very thick, ->] (0,0) -- (-1.5,-1.5) node[below left] {$\bar{q}$};

\node at (0,-2.7) {2-jet limit};
\end{scope}

\begin{scope}[xshift=8.5cm]
\node at (0,2.5) {$\tau \to \tfrac12$};

\draw[thick] (-3.5,0) -- (3.5,0);
\node[left]  at (-3.5,0) {$e^+$};
\node[right] at (3.5,0) {$e^-$};

\fill (0,0) circle (2pt);

\foreach \angle in {-150,-120,-90,-60,-30,0,30,60,90,120,150} {
  \draw[thick, ->] (0,0) -- ({2*cos(\angle)},{2*sin(\angle)});
  \draw[thick, ->] (0,0) -- ({-2*cos(\angle)},{2*sin(\angle)});
}

\node at (0,-2.7) {spherically distributed events};
\end{scope}
\end{tikzpicture}
\caption{Diagrammatic representation of thrust: $\tau \to 0$ corresponds to a pencil-like 2-jet configuration, while $\tau \to 1/2$ corresponds to a spherically distributed, multi-particle final state.}
\label{fig:thrust}
\end{figure}
\add{Maybe make the 2 jet limit arrows a bit shorter.}

\subsection{Thrust: Fixed-order Calculation}
\label{subsec:thrust-example}
For the process $e^+e^- \to q\bar{q}$, the final state consists of two back-to-back partons, yielding the Born-level contribution
\begin{equation}
    \frac{1}{\sigma_0}\frac{d\sigma}{d\tau} = \delta(\tau),
\end{equation}
where $\sigma_0$ denotes the LO cross-section given in Eq.~\eqref{eq:sigma-LO}. \\ \\
The first non-trivial contribution arises from the real-emission process $e^+e^- \to q\bar{q}g$. For a three-particle final state, thrust is determined by the largest momentum fraction,
\question{Should we be referencing the full cross-section example more? Also should we call it NLO real emission here? Different references say LO/NLO? Is there a difference with that and zeroth/1st order? Unsure what to call them basically.}
\begin{equation}
T = \max[x_q, x_{\bar{q}}, x_g],
\end{equation}
with $T_\text{min} = 2/3$ for equal momenta and $T_\text{max} = 1$. The corresponding differential distribution takes the form
\begin{equation}
	\frac{1}{\sigma_0}\frac{d\sigma}{dT} = C_F \frac{\alpha_s}{2\pi} \int dx_q \int dx_{\bar{q}} \frac{x_q^2 + x_{\bar{q}}^2}{(1-x_q)(1-x_{\bar{q}})} \, \delta\big(T - \max[x_q, x_{\bar{q}}, x_g]\big),
\end{equation}
\question{Same limits as in Sec.~\ref{sec:divergences} - also should we be referring to the equation in this section to give insight into where this equation came from?}
subject to the kinematic constraint $x_q + x_{\bar{q}} + x_g = 2$. Integrating over the available phase space yields
\begin{equation}
	\frac{1}{\sigma_0}\frac{d\sigma}{d\tau} = C_F \frac{\alpha_s}{2\pi} \left\{ \frac{3(1+\tau)(3\tau-1)}{\tau} + \frac{(4+6\tau(\tau-1))\ln\frac{1-2\tau}{\tau}}{\tau(\tau-1)} \right\}.
\end{equation}
As $\tau \to 0$, the distribution exhibits an IR divergence associated with soft and collinear emissions. This divergence is canceled by the virtual one-loop correction to $e^+e^- \to q\bar{q}$, in direct analogy with the cancellation discussed in Sec.~\ref{sec:divergences}, yielding a finite result as required by the IRC safety of thrust (Sec.~\ref{sec:IRC}). Using the regulated and integrated NLO cross-section given in Eq.~\eqref{eq:sigma-full}, the complete distribution may be written as \question{maybe reference Schwartz here?}
\begin{equation}
\begin{aligned}
\label{eq:full-thrust-NLO}
    \frac{1}{\sigma_0}\frac{d\sigma}{d\tau} =\;& \delta(\tau) + C_F\frac{\alpha_s}{2\pi} \Bigg\{ \delta(\tau) \left( \frac{\pi^2}{3} - 1 \right) \\
    & + \left[ 3(1+\tau)(3\tau-1) + \frac{(4+6\tau(\tau-1))\ln(1-2\tau)}{1-\tau}\right] \left( \frac{1}{\tau} \right)_+  \\
    & - \frac{4+6\tau(\tau-1)}{1-\tau} \left( \frac{\ln \tau}{\tau} \right)_+ \Bigg\}.
\end{aligned}
\end{equation}
Neglecting the $\delta$-function contributions, the terms that become large in the $\tau \to 0$ limit may be referred to as \emph{logarithmically enhanced terms},
\begin{equation}
	\left(\frac{1}{\sigma_0}\frac{d\sigma}{d\tau}\right)_{\text{log-enh}} = C_F \frac{\alpha_s}{2\pi} \left[ -3 \left(\frac{1}{\tau}\right)_+ - 4 \left(\frac{\ln \tau}{\tau}\right)_+ \right].
\end{equation}
The corresponding cumulative distribution (cumulant) is given by
\begin{equation}
\label{eq:thrust-cumulant}
	\Sigma_{\text{log-enh}}(\tau) 
	= \frac{1}{\sigma_0} \int_0^\tau d\tau' \frac{d\sigma}{d\tau'} 
	= C_F \frac{\alpha_s}{2\pi} 
	\left( -2L^2 + 3L \right),
\end{equation}
where $L \equiv \ln(1/\tau)$. All remaining terms in Eq.~\eqref{eq:full-thrust-NLO} remain finite \add{in the cumulant} as $\tau \to 0$ and do not contribute to the logarithmic enhancement. \\ \\
These logarithmically-enhanced terms cause issues. Physically, the divergence of the cumulant as $\tau \to 0$ signals the breakdown of the fixed-order expansion, since an exactly two-jet configuration is unphysical, corresponding to the observation of bare quarks. \add{Plot of experimental vs. theory FO.} At higher orders in perturbation theory, the cumulant contains towers of logarithms of the form \question{Unsure if we can say towers or if it is a bit misleading for other areas of QCD?}
\begin{equation}
\begin{aligned}
\Sigma(\tau)\supset\quad&\alpha_sL^2 \qquad \alpha_s L \qquad \alpha_s \\
&\alpha_s^2L^4 \qquad \alpha_s^2 L^3 \qquad \alpha_s^2 L^2 \qquad \alpha_s^2 L \qquad \alpha_s^2 \\
&\alpha_s^3 L^6 \qquad \alpha_s^3 L^5 \qquad \dots \\
&\,\,\vdots \\
&\alpha_s^n L^{2n}\qquad\dots \qquad \qquad \qquad  \qquad  \qquad  \qquad \qquad  \qquad \qquad \alpha_s^n
\end{aligned}
\end{equation}
\add{Still a bit unsure by this format, sort of loose the $\alpha_s^n$?}
which spoil the convergence of the perturbative series in the small-$\tau$ region, where the bulk of the data lies. \\ \\
To restore predictive power, the perturbative expansion must therefore be reorganised and \emph{resummed}. Rather than expanding solely in powers of the strong coupling $\alpha_s$, one performs an expansion in powers of the logarithms $L = \ln(1/\tau)$. This reorganisation becomes essential in the region $\tau \ll 1$, where the logarithms are large and $\alpha_s L \sim 1$, causing fixed-order perturbation theory to break down. This procedure, known as \emph{resummation}, systematically accounts for these enhanced contributions to all orders in $\alpha_s$, yielding an exponentiated form for the cumulant,
\begin{equation}
	\Sigma(\tau) \simeq 
	\exp \Big\{ 
	L g_1(\alpha_s L) 
	+ g_2(\alpha_s L) 
	+ \alpha_s g_3(\alpha_s L) 
	+ \cdots 
	\Big\},
	\label{eq:resummed-cummulant}
\end{equation}
where the functions $g_i$ resum logarithms at successive logarithmic accuracies. The leading-logarithmic contributions $\alpha_s^n L^{n+1}$ are contained in $g_1(\alpha_s L) $, next-to-leading logarithms $\alpha_s^n L^{n}$ in $g_2(\alpha_s L) $, and so on. \\ \\
\reference{~\cite{Luisoni:2015xha, Schwartz:2014sze, ParticleDataGroup:2024cfk}}


\section{Event-shapes}
The appearance of large logarithms and the need for resummation are not unique to thrust. In fact, the resummed distribution shown in Eq.~\eqref{eq:resummed-cummulant} applies to all IRC safe observables \question{rIRC, exclusive, global observables?}. Thrust is one such observable and belongs to a broader class known as \emph{event shapes}. Event shapes are IRC safe observables constructed from the four-momenta of final-state particles. They characterise the ``shape'' of an event by quantifying the geometrical distribution of its hadronic energy-momentum flow. While thrust provides a well-known example of an event shape in $e^+e^-$ collisions, event shapes are also widely used in hadronic collisions, including: \question{Correct description of event shape?} \\ \\
\question{The reference~\cite{Dasgupta:2003iq}, is a paper but was used merely for the background of event shapes - am unsure if necessary to cite or not?} 
\begin{itemize}
\item \textbf{Transverse thrust}, first introduced in Ref.~\cite{Banfi:2004nk}, is the extension of the $e^+e^-$ event-shape thrust, defined in Eq.~\eqref{eq:thrust}, to hadronic collisions. It is defined in the transverse plane as
\begin{equation}
  T_\perp \equiv \max_{\vec{n}_\perp}
  \frac{\sum_i \left| \vec{p}_{\perp i} \cdot \vec{n}_\perp \right|}
       {\sum_i \left| \vec{p}_{\perp i} \right|},
  \qquad
  \tau \equiv 1 - T_\perp \,,
\end{equation}
where $\vec{p}_{\perp i}$ denotes the transverse momentum of final-state particle $i$ with respect to the beam direction, and the unit vector $\vec{n}_\perp$ that maximises this quantity is referred to as the \emph{transverse thrust axis}.

\item \textbf{One-jettiness} is a special case of the more general $N$-jettiness event shape~\cite{Stewart:2010tn}, $\tau_N$, which quantifies how well the final state can be described by $N$ narrow jets. For $N=1$, one-jettiness is defined as
\begin{equation}
  \mathcal{T}_1 \equiv
  \sum_i
  \min\!\left[
    \frac{2 q_a \cdot p_i}{Q_a},
    \frac{2 q_b \cdot p_i}{Q_b},
    \frac{2 q_J \cdot p_i}{Q_J}
  \right] \,,
\end{equation}
where $q_{a,b,J}$ are the four-momenta of the incoming beams and the observed jet, and the sum runs over the final-state partons $p_i$. The normalisation factors are $Q_i = 2 \rho_i E_i$, with $\rho_i$ depending on the reference frame (e.g., $\rho_i = 1$ in the laboratory frame). While $\mathcal{T}_1$ is dimensionful, it is often convenient to define the dimensionless variable $\tau_1 \equiv \mathcal{T}_1 / Q$, where $Q$ is the hard scale of the interaction.

\item \textbf{Thrust minor}, also introduced in Ref.~\cite{Banfi:2004nk}, is a measure of out-of-plane radiation and is defined as
\begin{equation}
  T_m \equiv \frac{\sum_i |\vec p_{\perp i}\times \vec n_\perp|}{\sum_i p_{\perp i}}\,.
\end{equation}
\end{itemize}
\question{Correct description for all of them? \\ Are the references correct for transverse thrust and thrust minor?} \\ \\
Beyond their use in extracting parameters such as $\alpha_s$ and the QCD $\beta$-function, event shapes characterise the structure of final-state events. Event shapes provide detailed information on the distribution of energy and momentum, offering insight into features such as jet substructure. Because of their importance in probing QCD dynamics, accurate resummation techniques are essential. While many methods exist (see Sec.~\ref{subsec:QCD-ev}), much of the previous work on resumming event shapes has been observable-dependent. In this thesis, we focus on an observable-independent, \emph{semi-numerical} formalisms - \textsc{CAESAR} and \textsc{ARES}. We first review the \textsc{CAESAR} framework and then describe its extension to \textsc{ARES}. \\ \\
\reference{~\cite{Luisoni:2015xha, ParticleDataGroup:2024cfk, Banfi:2016yyq}}

\section{NLL Resummation: \textsc{CAESAR}}
Traditionally, NLL resummation for final-state observables was performed in an observable-dependent manner, with separate analytical calculations required for each individual case. The first steps towards a more general approach were taken in~\cite{Banfi:2001bz}, where a systematic semi-numerical framework was developed to perform NLL resummation for a broad class of observables. This framework was later extended and implemented in a computer code, the \emph{Computer Automated Expert Semi-Analytical Resummer} (\textsc{CAESAR})~\cite{Banfi:2004yd}. In this approach, the resummation formula is written in terms of a set of observable-dependent parameters, which can be determined automatically by the code for an arbitrary observable. \add{Need to become a bit clearer on the history of this -- unsure if really necessary. I think see the length and come back to it later.} We will now discuss the conditions an observable must satisfy in order for this resummation approach to be valid.

\subsection{Observable Requirements}
Consider a Born event with $n$ hard partons, where $p_1, \dots, p_n$ denote their momenta and $n_i$ are the incoming legs, we define an observable $V(p_1, p_2, \dots)$ as a non-negative function of the final-state momenta. We assume the observable is IRC safe and that, in the $n$-jet limit, it vanishes. Such an observable is referred to as an $(n+1)$-jet observable, where the incoming beam jets $n_i$ are included alongside the outgoing jets. For the resummation approach to be valid, the observable $V(p_1, p_2, \dots)$ must satisfy the following conditions: \question{Unsure about the convention here - think I need to change it since we are sticking to convention in Arpino paper.}
\begin{enumerate}
\item \textbf{Vanish smoothly}: when an additional emission of momentum $k$ is soft and collinear to a hard leg $\ell$. In this limit, the observable can be written as
\begin{equation}
	V(\{\tilde{p}\},k)\simeq V_{\text{sc}}(\{\tilde{p}\},k)\equiv d_\ell\left(\frac{k_t^{(\ell)}}{Q}\right)^a e^{-b_\ell\eta^{(\ell)}} g_\ell(\phi^{(\ell)})\,,
	\label{eq:general-sc-V}
\end{equation}
where $V_{\text{sc}}$ denotes the observable evaluated in the soft and collinear limit. The set $\{\tilde{p}\}$ represents the Born momenta after recoil from the emission $k$, whose transverse momentum, rapidity, and azimuthal angle with respect to the emitting leg $p_\ell$ are given by $k_t^{(\ell)}$, $\eta^{(\ell)}$, and $\phi^{(\ell)}$, respectively.  The energy $Q$ is an arbitrary hard scale of the process and serves as a reference normalisation for the transverse momenta. \\ \\
The parameters $a$, $b_\ell$, $d_\ell$, and the function $g_\ell(\phi)$ characterise the observable in this limit. Writing observables in this form allows these parameters to be extracted for each leg, enabling the construction of the resummation formula. Further details will be discussed later. IRC safety implies that $a>0$ and $b_\ell>-a$. \ABquestion{Should I put in the reference for the: $a>0$ and $b_\ell>-a$ comment, as it is in Refs.~\cite{Banfi:2004yd, Banfi:2003je}?}


\item \textbf{Recursively IRC (rIRC) safe}: this condition requires that, for an ensemble of soft and collinear emissions, the addition of a further emission that is relatively much softer and/or collinear should not significantly modify the value of the observable. A formal definition of rIRC safety is given in Refs.~\cite{Banfi:2004yd, Banfi:2003je} and is summarised below.
 Consider momenta $\kappa_i(\lambda_i)$ such that the observable satisfies
\begin{equation}
\label{eq:rIRC1}
	V(\{\tilde{p}\},\kappa_i(\lambda_i))=\lambda_i\,,
\end{equation}
where $\lambda_i\to0$ in the soft and/or collinear limits, while the azimuthal angles $\phi_i$ of the emissions $\kappa_i(\lambda_i)$ are fixed. The functions $\kappa_i(\lambda_i)$ may differ for each emission but must satisfy Eq.~\eqref{eq:rIRC1}. The conditions for rIRC safety are then:
\begin{enumerate}
	\item The following limit must be well-defined and non-zero:
	\begin{equation}
	\lim_{\varepsilon\to0}\frac{1}{\varepsilon}V(\{\tilde{p}\},\kappa_1(\varepsilon\lambda_1),\dots,\kappa_m(\varepsilon\lambda_m))\,,
	\end{equation}
	which can be interpreted as requiring the soft and collinear scaling of the observable to be independent of the number of emissions.
	\item The following equality must hold:
	\begin{equation}
	\begin{aligned}
	\lim_{\lambda_{m+1}\to0}\lim_{\varepsilon\to0}\frac{1}{\varepsilon}V(&\{\tilde{p}\},\kappa_1(\varepsilon\lambda_1),\dots,\kappa_m(\varepsilon\lambda_m), \kappa_{m+1}(\varepsilon\lambda_{m+1}))\\
		&= \lim_{\varepsilon\to0}\frac{1}{\varepsilon}V(\{\tilde{p}\},\kappa_1(\varepsilon\lambda_1),\dots,\kappa_m(\varepsilon\lambda_m))\,,
	\end{aligned}
	\end{equation}
	showing that the addition of an additional much softer and/or collinear emission must not affect the value of the observable.
\end{enumerate}
\item \textbf{Continuously global}: an observable is said to be global if it is sensitive to emissions in all regions of phase space, i.e.\ it is non-zero for any emission of an $(n+1)^{\text{th}}$ parton that is not infinitely soft or collinear. Continuous globalness further requires that, for a single soft emission, the parametric dependence of the observable on the emission’s transverse momentum be uniform across phase space, implying that the parameter $a$ appearing in Eq.~\eqref{eq:general-sc-V} must be the same for each leg. This condition ensures that non-global logarithms, which arise when different regions of phase space exhibit different transverse-momentum dependences, do not appear. While these types of logarithms will not be discussed in this thesis, further discussion can be found in~\cite{Dasgupta:2001sh,Dasgupta:2002dc}. \ABquestion{Are these the correct references? Or are there more suitable ones?} \ABquestion{Have I got the right end of the stick with global, continuously global, and non-global logarithms?}
\end{enumerate}
\reference{~\cite{Banfi:2004yd, Banfi:2003je}.}


\subsection{Formalism}
To illustrate the \textsc{CAESAR} and \textsc{ARES} formalisms, we consider the specific example of three-jet event shapes in $e^+e^-$ collisions in the near-to-planar limit~\cite{Arpino:2019ozn}. The motivation for focusing on this process is that the analysis presented in Chapter~5 (hadronic $Z+\text{jet}$ production) is largely based on the \textsc{ARES} formalism for this process. Although Ref.~\cite{Arpino:2019ozn} does not present a detailed derivation of the original \textsc{CAESAR} framework, the NLL construction can be obtained from the general procedure described in Refs.~\cite{Banfi:2004yd, Banfi:2003je}. \ABquestion{I want to make it clear that we use the final-state resummation here for the soft and final-state hard-collinear radiation but unsure if it is the best place to?}

\subsubsection{Kinematics}
At Born level, a three-jet event in $e^+e^-$ annihilation consists of a quark, anti-quark, and gluon with momenta
\begin{equation}
\label{eq:3-born-kin}
	p_1=E_1(1,0,0,1)\,,\quad p_2=E_2(1,0,\sin\theta_{12},\cos\theta_{12})\,,\quad p_3=E_3(1,0,-\sin\theta_{13},\cos\theta_{13})\,,
\end{equation}
where $p_1$, $p_2$, and $p_3$ correspond to the quark, anti-quark, and gluon, respectively, and $\theta_{ij}$ denotes the angle between momenta $p_i$ and $p_j$.

\subsubsection{Selection of three-jet events}
We consider an observable that satisfies the requirements outlined previously, namely that it is rIRC safe, continuously global, and vanishes in the three-jet limit,
\[
V(\{\tilde{p}_1,\tilde{p}_2,\tilde{p}_3\}) = 0\,.
\]
To study event shapes in the near-to-planar region, we require a procedure that selects hadronic events containing three or more well-separated hard jets. This can be achieved, for example, through a jet algorithm that counts the number of jets in the event, or by imposing a cut on some secondary, two-jet observable. We can represent this constraint through a selection function $\mathcal{H}(p_1,\dots,p_n)$, defined in terms of the hadron momenta $p_1,\dots,p_n$, such that
\begin{equation}
\mathcal{H}(p_1,\dots,p_n)=
\begin{cases}
1 & \text{if the event satisfies the selection criteria},\\
0 & \text{otherwise}.
\end{cases}
\end{equation}
The total cross section for events satisfying this selection - i.e. the total 3-jet cross section is defined by
\begin{equation}
	\sigma_{\mathcal{H}}\equiv\sum_{n=3}^\infty\int d\Phi_n\frac{d\sigma_n}{d\Phi_n}\mathcal{H}(p_1,\dots,p_n)\,,
\end{equation}
where $d\sigma_n/d\Phi_n$ is the differential cross section for producing $N$ final-state particles. \question{Define the phase space here also/refer to where it is in Ch2?} 
The corresponding cumulant, restricted both by the event selection and by the requirement that the observable be smaller than some value $v$, is
\begin{equation}
	\Sigma_{\mathcal{H}}(v)=\sum_{n=3}^{\infty}\int d\Phi_n\frac{d\sigma_n}{d\Phi_n}\Theta\left(v-V(p_1,\dots,p_n)\right)\mathcal{H}(p_1,\dots,p_n)
\end{equation}
In the near-to-planar region, where $v\ll1$, the cumulant takes a factorised form,
\begin{equation}
\Sigma_{\mathcal{H}}\simeq\frac{1}{\sigma_{\mathcal{H}}}\int d\Phi_3\frac{d\sigma_3}{d\Phi_3}\Sigma\left(\{p_1,p_2,p_3\},v\right)\mathcal{H}(p_1,p_2,p_3)\,,
\end{equation}
where $p_1$, $p_2$, and $p_3$ are the Born momenta given in Eq.~\eqref{eq:3-born-kin}, and the explicit expression for $d\sigma_3/d\Phi_3$ can be found in Ref.~\cite{Arpino:2019ozn}. \\ \\
As discussed previously in Sec.~\ref{subsec:thrust-example} for the case of thrust, in the limit $v\ll1$ the logarithms $L\equiv\ln(1/v)$ appearing in the function $\Sigma\left(\{p_1,p_2,p_3\},v\right)$ become large. It is this function that must be resummed to a given logarithmic accuracy - NLL in the case of \textsc{CAESAR} - to all orders in $\alpha_s$. \\
\ABquestion{Should I be discussing recoil a bit more?} \\ 
\ABquestion{I need to understand how these equations are derived actually? Also need to understand difference between $\sigma$ and $\sigma_{\mathcal{H}}$? Is $\sigma(v)$ the one defined in Eq.~\eqref{eq:thrust-cumulant}?}\\
\ABquestion{Empathise a bit more that \emph{this} is the function we are resumming?}
\reference{\cite{Arpino:2019ozn}.}


\subsubsection{NLL Resummation}
In the limit $v\to 0$, the cumulant for a three-jet observable can be written in factorised form as
\begin{equation}
\label{eq:3jet-cumu-gen}
\Sigma\left(\{p_1,p_2,p_3\},v\right)=\mathcal{V}(\{p\})\sum_{n=0}^\infty S(n) \int\left(\sum_{i=1}^n\left[dk_i\right]\right)\mathcal{M}^2\left(\{\tilde{p}\},k_1,\dots,k_n\right)\Theta\!\left(v-V\left(\{\tilde{p}\},k_1,\dots,k_n\right)\right),
\end{equation}
where $S(n)$ is a symmetry factor (equal to $1/n!$ for $n$ identical gluons), $\mathcal{V}(\{p\})$ denotes the virtual corrections to the Born process normalised to the Born matrix element squared, and $\mathcal{M}^2$ are the real corrections. \question{Make it clear that $M^2$ is just $|M|^2$?} \\ 
At NLL, one can simply consider an ensemble of soft-collinear partons, which are emitted independently off the hard legs, together with the virtual corrections. In this limit, $[dk]\,\mathcal{M}^2(k)$ reduces to the one-gluon emission probability \ABquestion{single soft-collinear emission probability}, whose explicit form can be found in~\cite{Banfi:2004yd}.  \\ \\
Therefore, at NLL accuracy, the cumulant for a three-jet observable takes the form
\begin{equation}
\label{eq:NLL-cumulant}
\Sigma\left(\{p_1,p_2,p_3\},v\right)=e^{-R_{\mathrm{NLL}}(v)}\mathcal{F}_{\mathrm{NLL}}\left(R'_{\mathrm{NLL}}(v)\right)\,,\quad R'_{\mathrm{NLL}}\simeq-v\frac{dR_{\mathrm{NLL}}}{dv}\,,
\end{equation}
where $R_{\mathrm{NLL}}(v)$ is the NLL radiator, encoding the probability of no emissions with $V(\{\tilde{p}\},k_i)>v$. The derivative $R'_{\mathrm{NLL}}$ includes all logarithmic contributions \question{make it clear that it is the log contributions up to NLL of the radiator i.e. just neglecting terms above NLL} up to NLL accuracy, neglecting terms beyond this order.\\ 
For a three-jet configuration, the NLL radiator is a sum over the dipoles $(q\bar q)$, $(qg)$ and $(g\bar q)$,
\begin{equation}
\label{eq:NLL-radiator}
\begin{split}
	R_{\mathrm{NLL}}(v)=\sum_{(ij)\in\{(q\bar{q}), (qg), (g\bar{q})\}}C_{(ij)}\left(\sum_{\ell=i,j}\left[r_\ell(L)+r'_\ell(L)\left(\langle \ln(d_\ell g_\ell)\rangle - b_\ell\ln\frac{2E_\ell}{Q}\right) \right. \right. & \\
	\left. \left. +B_\ell T\left(\frac{L}{a+b_\ell}\right)\right] +2\ln\frac{Q_{ij}}{Q}T\left(\frac{L}{a}\right)\right)\,,
\end{split}
\end{equation}
where $L=\ln(1/v)$, $Q_{ij}^2=2p_i\!\cdot p_j$, and $C_{(ij)}$ is the colour factor of the dipole,
\begin{equation}
C_{(ij)}=
\begin{cases}
2C_F-C_A\,, & q\bar{q}\,,\\
C_A\,,& qg,\,g\bar{q}\,.
\end{cases}
\end{equation}
Explicit expressions for the functions $r_\ell(L), r'_\ell(L)$ and $T(L)$ can be found in~\cite{Arpino:2019ozn}. \\
To define $\mathcal{F}_{\mathrm{NLL}}$, each emission $k_i$ is parametrised by $\{\ell_i,\phi_i^{(\ell_i)},\zeta_i\}$, 
where; $\ell_i$ denotes the leg to which the emission is collinear, $\phi_i^{(\ell_i)}$ is the azimuthal angle with respect to that leg, and
\[
\zeta_i \equiv \frac{V_{\mathrm{sc}}(k_i)}{v}.
\]
With this parametrisation,
  \begin{equation}
    \label{eq:FNLL-formula}
    \mathcal{F}_{\mathrm{NLL}}\left(R'_{\mathrm{NLL}}(v)\right)= \int\! \dZ \,\Theta\left(1-\frac{\Vsc{\{k_i\}}}{v}\right)\,.
  \end{equation}
where the soft-collinear measure is defined as
  \begin{multline}
    \label{eq:dZ-def}
    \int\! \dZ \,G(\{k_i\}) \\ \equiv \lim_{\epsilon\to 0} \epsilon^{\RpNLL} \sum_{n=0}^\infty\frac{1}{n!} \int \prod_{i=1}^n \left(\sum_{\ell_i} R'_{\text{NLL},\ell_i}\int_{\epsilon}^\infty \frac{d\zeta_i}{\zeta_i}\int_0^{2\pi}\frac{d\phi_i^{(\ell_i)}}{2\pi}\right)G(k_1,\dots,k_n)\,.
  \end{multline}
The parameter $\epsilon$ acts as a cutoff, where emissions with $\zeta_i<\epsilon$ are \emph{unresolved} and, together with virtual corrections, generate the factor $\epsilon^{R'_{\mathrm{NLL}}}$. The soft-collinear approximation to the observable is defined as
\begin{equation}
  \label{eq:Vsc}
  \frac{\Vsc{k_1,\dots,k_n}}{v}\equiv \lim_{v\to 0}\frac{V({\{\tilde{p}\},k_1,\dots,k_n})}{v}\,,
\end{equation}
which corresponds to approximation of the observable when all emissions $k_1,\dots,k_n$ are simultaneously soft and collinear. Recursive IRC safety guarantees that this limit exists.
For event shapes, the dependence on emission rapidity can be integrated analytically, which in turn can yield a Jacobian factor $R'_{\mathrm{NLL},\ell} \equiv 2 C_\ell\, r'_\ell(L)$, with $C_\ell=C_F$ for quark legs and $C_\ell=C_A$ for gluon legs. 
\ABquestion{Mention more about resolved/unresolved?}

\section{NNLL Resummation: ARES}
The \textsc{ARES} formalism was first developed for two-jet observables in $e^+e^-$ collisions in Ref.~\cite{Banfi:2014sua}, with an extension to jet-jet rates in Ref.~\cite{Banfi:2016zlc}. Moreover, the NNLL Sudakov radiator for observables with two hard legs at Born level was formulated in~\cite{Banfi:2018mcq}. The extension to the NNLL resummation for 3-jet observables  was first introduced in~\cite{Arpino:2019ozn}.  In this section, we will discuss the necessary ingredients required to form the NNLL cumulant.

\subsection{The master formula}
The general idea behind determining the cumulant at NNLL accuracy is to collect all contributions that give rise to a contribution of order $\alpha_s$ compare to the NLL prediction. As discussed in Refs.~\cite{Banfi:2014sua, Arpino:2019ozn}, this can be done simply by relaxing the NLL approximations and in this way can be done for a single real emission at a time. For configurations where the correction is for more than one emission, it will extend beyond NNLL accuracy and so can therefore be neglected. Using these ideas, along with the explicit derivations found in Ref.~\cite{Banfi:2018mcq}, the NNLL master formula is defined as
\begin{equation}
\begin{aligned}
  \label{eq:Sigma-NNLL-fs}
  \Sigma(\{p_1,p_2,p_3\},v)&=\; e^{-R_{\mathrm{NNLL}}(v)}\,\times
 \\
& \times \left(\mathcal{F}_{\mathrm{NLL}}(\lambda)\left(1+\frac{\alpha_s(Q)}{2\pi}H^{(1)}
      +\sum_{\ell=1}^3\frac{\alpha_s(Qv^{\frac{1}{a+b_\ell}})}{2\pi}C_{\mathrm{hc},3}^{(1)}\right) +\frac{\alpha_s(Q)}{\pi} \delta\mathcal{F}_{\mathrm{NNLL}}(\lambda)\right)\,,
 \end{aligned}
\end{equation}
where $\lambda\equiv \alpha_s(Q)\beta_0\ln(1/v)$ and $\beta_0$ is defined in Eq.~\eqref{eq:beta-coeff}. Note that both $\mathcal{F}_{\mathrm{NLL}}$ and $\delta\mathcal{F}_{\mathrm{NNLL}}$ are single logarithmic functions and can therefore be defined in terms of the quantity $\lambda$. We will now give a brief summary and description of the ingredients in this formula:
\begin{itemize}
\item The NNLL radiator, $R_{\mathrm{NNLL}}(v)$, includes all virtual corrections of soft and/or collinear origin. These are divergent, but their divergences cancel with those of real radiation. At the scope, the radiator includes also a subset of real corrections for suitably defined \emph{unresolved} emissions. The procedure to define those emissions is not unique. The explicit formula of $R_{\mathrm{NNLL}}(v)$ for three emitting legs can be found explicitly in Ref.~\cite{Arpino:2019ozn}. Note that its expression depends only on the observable properties with a single soft and collinear emission, more precisely $a,b_\ell,d_\ell,g_\ell(\phi)$. It should be noted that, with the knowledge we have acquired so far, the radiator could be computed for a generic observable at N$^3$LL accuracy. \ABquestion{Unsure about the resolved emissions part/ master formula part in hhZjet tex.}

\item The NLL function $\mathcal{F}_{\mathrm{NLL}}(\lambda)$, given in Eq.~\eqref{eq:FNLL-formula}, as discussed before come from \emph{resolved} soft and collinear emissions widely separated in angle, along with the the corresponding virtual corrections.

\item Finite virtual corrections of relative order $\alpha_s$ are embedded in the coefficient $H^{(1)}$. This is a term of order $\alpha_s$ multiplying a NLL function, hence NNLL. The explicit formula for this can be found in Ref.~\cite{Arpino:2019ozn}.

\item  A single unresolved hard collinear emission emitted from one of the three final-state legs $p_\ell$ combines with the corresponding virtual corrections to give the coefficient $C_{\mathrm{hc},\ell}^{(1)}$, and a NNLL contribution. The explicit expression of $C_{\mathrm{hc},\ell}^{(1)}$ depends on the flavour of leg the emission is emitted from. \ABquestion{I am unsure whether what I have written is correct for $e^+e^-$ case?} For a quark or an antiquark
  \begin{equation}
    \label{eq:C1hc-q}
    C_{\mathrm{hc},\ell}^{(1)}=C_F\left(\frac{7}{2}\frac{b_\ell}{a+b_\ell}+\frac{3}{a+b_\ell}\left(\ln\langle d_\ell g_\ell\rangle-b_\ell\ln\frac{2E_\ell}{Q}\right)+\frac 12\right)\,,
  \end{equation}
  while for a gluon
  \begin{equation}
    \label{eq:C1hc-q}
    \begin{split}
    C_{\mathrm{hc},\ell}^{(1)}& =\left(\frac{67}{18}C_A-\frac{13}{9}T_R n_f\right)\frac{b_\ell}{a+b_\ell}\\ & +\frac{1}{a+b_\ell}\left(\frac{11}{3}C_A-\frac{4}{3}T_R n_f\right)\left(\ln\langle d_\ell g_\ell\rangle-b_\ell\ln\frac{2E_\ell}{Q}\right)+\frac{1}{3}T_R n_f\,.
    \end{split}
  \end{equation}
\item All other soft or hard-collinear emissions and the
  corresponding virtual corrections build the NNLL function
  $\delta\mathcal{F}_{\mathrm{NNLL}}(\lambda)$. More precisely, these
  consist of a single soft and/or collinear \emph{special} emission,
  accompanied by an arbitrary number of soft and collinear emissions
  widely separated in angle (and the corresponding virtual
  corrections). The function $\delta\mathcal{F}_{\mathrm{NNLL}}(\lambda)$
  is the sum of various terms, which reflect the region of phase space
  spanned by the special emission, namely:
  \begin{equation}
    \label{eq:dFNNLL}
    \delta\mathcal{F}_{\mathrm {NNLL}} = \delta\mathcal{F}_{\mathrm{sc}}+\delta\mathcal{F}_{\mathrm{hc}}+\delta\mathcal{F}_{\mathrm{rec}}+\Delta\mathcal{F}_{\mathrm{rec}}+\delta\mathcal{F}_{\mathrm{wa}}+\Delta\mathcal{F}_{\mathrm{wa}}+\delta\mathcal{F}_{\mathrm{correl}}\,.
  \end{equation}
  The meaning of the various contributions will become clear in the following section. Please note that in the 2-jet case~\cite{Banfi:2014sua}, the terms $\Delta\mathcal{F}_{\mathrm{wa}}$ and $\Delta\mathcal{F}_{\mathrm{rec}}$ are not present and were introduced for the first time in the 3-jet case in Ref.~\cite{Arpino:2019ozn}. All of the terms in Eq.~\eqref{eq:dFNNLL} can be expressed in terms of integrals over the soft-colliner measure $\dZ$
  of the appropriate weight function $G(\{k_i\})$, see
  Eq.~\eqref{eq:dZ-def}. \ABquestion{There are resolved emissions also? Also are they soft and hard-collinear or soft and collinear?}
\end{itemize}
In the following sections, we will provide a description for the majority of terms making up the $\delta\mathcal{F}_{\mathrm{NNLL}}(\lambda)$ function. We will not provide explicit expressions for $H^{(1)}$ and $R_{\mathrm{NNLL}}(v)$, and ask the reader to refer to Ref.~\cite{Arpino:2019ozn} for details, since these will change when discussing hadronic collisions for 1 jet observables in Chapter.~5 and are simply introducing the other terms as they will be discussed in the following chapter.

\subsection{Soft and collinear contributions}
\label{sec:sc-contributions}
\todo{Look over this and do again.}
In this section we describe all contributions arising from soft and
collinear emissions, as stated in the title. These are
$\mathcal{F}_{\mathrm{NLL}}$, $\delta \mathcal{F}_{\mathrm{sc}}$ and
$\delta \mathcal{F}_{\mathrm{hc}}$, for which all gluons are widely
separated in angle, and $\delta \mathcal{F}_{\mathrm{correl}}$ where one
soft and collinear gluon splits into $q\bar{q}$ or a gluon pair. Note that all these
functions require to the approximation shown in Eq.~\ref{eq:Vsc}. \\ \\
The soft and collinear corrections which
contribute to the NNLL resummation are, in order of complexity,
$\delta \mathcal{F}_{\mathrm{hc}}$, $\delta \mathcal{F}_{\mathrm{sc}}$, and
$\delta \mathcal{F}_{\mathrm{correl}}$. On top of the soft-collinear
emissions $k_1,\dots,k_n$ that are needed for any resummation, the two
functions $\delta \mathcal{F}_{\mathrm{hc}}$, $\delta \mathcal{F}_{\mathrm{sc}}$
involve the presence of an additional soft and collinear emission $k$,
parametrised in terms of the usual variable $\zeta=\Vsc{k}/v$, as well
as its emitter's leg $\ell$, and azimuthal angle $\phi^{(\ell)}$. For
$\delta \mathcal{F}_{\mathrm{correl}}$, this additional emission is
required to split into $k_a$ and $k_b$, with further dimensionsless
variables parametrising the splitting (see later). We now compute each
of these functions in turn, for the three considered observables.
 The general expression for $\delta \mathcal{F}_{\mathrm{hc}}$ is given by \ABquestion{check prefactors}
 \begin{multline}
   \label{eq:dFhc-general}
   \delta \mathcal{F}_{\mathrm{hc}}(\lambda) \equiv \int_0^\infty\frac{d\zeta}{\zeta}\sum_\ell \frac{\alpha_s\left(v^{\frac{1}{a+b_\ell}}Q\right)}{(a+b_\ell)\alpha_s(Q)}\gamma_\ell^{(0)} \int_0^{2\pi}\frac{d\phi^{(\ell)}}{2\pi} \int\! \dZ \,\times \\ \times \left[\Theta\left(1-\frac{\Vsc{k,\{k_i\}}}{v}\right)
       -\Theta\left(1-\frac{\Vsc{\{k_i\}}}{v}\right)\Theta(1-\zeta)
     \right] \,.
 \end{multline}







\subsection{Hard and collinear contributions}

\subsection{Soft wide angle contributions}

The corrections, $\delta\mathcal{F}_{\mathrm{sc}}, \delta\mathcal{F}_{\mathrm{wa}}, \Delta\mathcal{F}_{\mathrm{wa}}, \delta\mathcal{F}_{\mathrm{correl}}$ all have origin in resolved soft emissions, whereas the terms $\delta\mathcal{F}_{\mathrm{hc}}, \delta\mathcal{F}_{\mathrm{rec}}, \Delta\mathcal{F}_{\mathrm{rec}}$ have hard-collinear nature. 
% Each resolved real emission is found by requiring that $V(\{\tilde{p}\},k_i)>\epsilon v$ such that there is a lower bound on the phase-space where a resolved emission can take place and because of rIRC safety, $\epsilon$ is independent of $v$. 

\begin{enumerate}
\item[Soft emissions]: $\delta\mathcal{F}_{\mathrm sc}$ represents the running coupling corrections to the real emissions in the CMW scheme. It also accounts for the correct rapidity boundary for a single soft-collinear emission. $\delta\mathcal{F}_{\mathrm wa}$ arises from a soft emission near $\eta=0$ and accounts for the variation between the observable and its soft-collinear parametrisation for a single soft wide-angle emission accompanied by an ensemble of soft-collinear gluons. $\Delta\mathcal{F}_{\mathrm wa}$ is the NNLL contribution arising from wide angled resolved emissions. Finally, $\delta\mathcal{F}_{\mathrm correl}$ addresses the treatment of the splitting of a single soft and collinear gluon.
\item[Hard-collinear emissions]: $\delta\mathcal{F}_{\mathrm hc}$ originates from the emission of a hard-collinear parton at the level of the squared matrix element. $\delta\mathcal{F}_{\mathrm rec}$ is due to a hard-collinear emission (at the level of the kinematics) recoiling against the soft-collinear ensemble of emissions. This includes contributions due to spin correlations and hence may depend on the underlying Born momenta. $\Delta\mathcal{F}_{\mathrm rec}$ appears for the first time in near-to-planar three jet events. This accounts for correlations which occur between the spin of a hard collinear gluon and the event plane. 
\end{enumerate}

\section{Validation and Matching}
\emph{Include something on matching with fixed order i.e. something to essentially show that resummation is necessary. I think here we can add in some of the plots calculated from the numerical work done in hhHjet project.}